os.loadAPI("reactor_api")
os.loadAPI("reactor_display")


local r = reactor_api.Reactor:new("BigReactors-Reactor_1")

while true do
  r:adjust_control_level()
  print("Efficiency: " .. tostring(r:get_efficiency()) .. " RF/mB")

  r:update_state()
  os.sleep(1)
end
