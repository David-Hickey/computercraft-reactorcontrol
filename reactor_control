os.loadAPI("reactor")


local r = reactor.Reactor:new("BigReactors-Reactor_1")

while true do
  r:adjust_control_level()
  print("Efficiency: " .. tostring(r:get_efficiency()) .. " RF/mB")

  r:update_state()
  os.sleep(1)
end
